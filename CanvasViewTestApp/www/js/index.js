document.addEventListener('deviceready', init);

function init() {
    console.log('Loaded!');
    // document.body.addEventListener("click", function () {
    //     new CanvasView.View(document.getElementById("map"), 'test', true);
    // });
}
